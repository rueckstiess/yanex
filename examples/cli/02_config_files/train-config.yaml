# Configuration for model training experiment

model:
  learning_rate: 0.001
  hidden_size: 128
  dropout: 0.1

training:
  epochs: 10
  batch_size: 32
  optimizer: adam

data:
  dataset: mnist
  train_split: 0.8
  validation_split: 0.2
